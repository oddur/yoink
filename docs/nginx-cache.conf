# Browser caches briefly; Cloudflare caches longer. Both invalidate
# fast enough that a redeploy is visible in minutes, not hours. The
# `always` flag emits the header on error responses too, which matters
# for 404s on stale links.
add_header Cache-Control "public, max-age=60, s-maxage=300" always;
